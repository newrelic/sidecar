#
# DO NOT EDIT THIS FILE
# auto-generated by Bosun at {{ now }} 
#

global
	daemon
	user haproxy
	group haproxy
	maxconn 4096
	log     127.0.0.1 local0
	log     127.0.0.1 local1 notice
	stats   socket /var/run/haproxy_stats.sock mode 666 level admin

defaults
	log      global
	option   dontlognull
	maxconn  4096
	retries  3
	timeout  connect 5s
	timeout  client  1m
	timeout  server  1m
	option   redispatch
	balance  roundrobin

listen stats :3212
	mode http
	stats enable
	stats uri /
	stats refresh 5s

{{ range $svcName, $services := .Services }} {{ range $port := getPorts $svcName }}
# ----------- {{ $svcName }} port {{ $port }} --------------
frontend {{ $svcName }}-{{ $port }}
	mode http
	bind {{ bindIP }}:{{ $port }}
	default_backend {{ $svcName }}

backend {{ $svcName }}-{{ $port }}
	mode http {{ range $services }}
	server {{ .Name }} {{ .Hostname }}:{{ $port }} cookie {{ .Name }}-{{ $port }} {{ end }}
{{ end }}
{{ end }}
